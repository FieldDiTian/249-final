# ME249 Project 3 – Task 3.2.5（六边形域）

## 新建 Notebook
- 文件：`CodeP3.4F25_task3_2_5.ipynb`（基于 3.2.3 流程，独立，不修改原代码）。
- 输出目录：`outputs/part4`（图像/指标前缀含 part4）。

## 修改内容（相对 3.2.3）
- 点集调整（六边形域）：将以下 6 点从内部 PDE 列表移除并加入边界列表：  
  - 热侧（T=50°C）：[0.5, 1.0], [0.5, 0.5], [1.0, 0.5]  
  - 冷侧（T=10°C）：[2.0, 4.5], [2.5, 4.5], [2.5, 4.0]  
  内部点由 45→39；边界点增加 6。
- 数据更新：在 xdata/ydata 中追加上述 6 点及对应温度（50, 50, 50, 10, 10, 10）。
- 模型/训练：5-10-1 (elu)，Adam lr=2e-4，kappa=5.0，epochs=25000，batch_size=32，N_epInit=2。

## 运行与输出
- 训练：运行 notebook 前 4 个单元（数据/模型/损失/训练），目标 loss < 0.005。
- 指标：自动写入 `outputs/part4/metrics_hex_kappa5.json`（best_epoch, best_loss）。
- 图像：`outputs/part4/part4_lr2e-4_kappa5.0_scatter.png`、`..._surface.png`、`..._heatmap.png`。

## 本次运行结果（首次）
- 参数：5-10-1 (elu)，kappa=5.0，Adam lr=2e-4，epochs=25000，batch=32，N_epInit=2。
- 指标：best_epoch = 24955，best_loss ≈ 0.49575（未达到 <0.005 目标）。
- 图像：请运行绘图单元保存到 `outputs/part4/part4_lr2e-4_kappa5.0_{scatter,surface,heatmap}.png`。
- 评估：当前 loss 仍偏高，需进一步调参（可尝试减小学习率至 1e-4 或增大网络容量/epochs），再与 3.2.3 曲面对比评估相似度。***

## 第二次
- 参数：5-10-1 (elu)，kappa=1.3，Adam lr=2e-4，epochs=25000，batch=32，N_epInit=2。
best epoch = 24993
smallest loss = 0.9617307782173157
## 第二次
- 参数：5-10-1 (elu)，kappa=1.3，Adam lr=1e-4，epochs=25000，batch=32，N_epInit=2。

best epoch = 24997
smallest loss = 1.7413606643676758
2/2 [==============================] - 0s 782us/step

## 第三次

- 参数：5-10-1 (elu)，kappa=1.3，Adam lr=1e-5，epochs=25000，batch=32，N_epInit=2。

best epoch = 25000
smallest loss = 615.1560668945312
2/2 [==============================] - 0s 668us/step

## 第四次
- 参数：5-10-1 (elu)，kappa=3.0，Adam lr=2e-4，epochs=40000，batch=32，N_epInit=2。


best epoch = 39993
smallest loss = 0.09475932270288467

##第五次
参数同上 - 参数：5-10-1 (elu)，kappa=3.0，Adam lr=2e-4，epochs=40000，batch=32，N_epInit=2。

best epoch = 39995
smallest loss = 0.18148259818553925

## 第六次
- 参数：5-10-1 (elu)，kappa=4.0，Adam lr=2e-4，epochs=40000，batch=32，N_epInit=2。

best epoch = 39980
smallest loss = 0.08324284851551056

## 第六次
- 参数：5-10-1 (elu)，kappa=4.0，Adam lr=1e-4，epochs=40000，batch=32，N_epInit=2。

est epoch = 39948
smallest loss = 1.1355010271072388

## 第七次
- 参数：5-10-1 (elu)，kappa=4.0，Adam lr=2e-3，epochs=40000，batch=32，N_epInit=2。

best epoch = 39705
smallest loss = 0.07621214538812637
## 第八次
- 参数：5-10-1 (elu)，kappa=4.0，Adam lr=9e-3，epochs=40000，batch=32，N_epInit=2。

best epoch = 39121
smallest loss = 0.0036639112513512373